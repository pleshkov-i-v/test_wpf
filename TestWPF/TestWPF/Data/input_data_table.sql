CREATE TABLE [InputData] (
  [A] TEXT NOT NULL
, [B] TEXT NOT NULL
, [C] TEXT NOT NULL
, [D] TEXT NOT NULL
, [E] TEXT NOT NULL
, [F] REAL NOT NULL
, [G] TEXT NULL
, [H] TEXT NOT NULL
, CONSTRAINT [PK_InputData] PRIMARY KEY ([A])
);
